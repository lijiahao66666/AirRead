org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true
# Force Gradle to use a specific JDK if available, though jlink usually uses the same as Daemon.
# The error suggests core-for-system-modules.jar (Android 34) is incompatible with JDK 21 jlink?
# Or the cache is corrupted. Since I cannot delete cache outside, try disabling daemon reuse?
org.gradle.daemon=false
